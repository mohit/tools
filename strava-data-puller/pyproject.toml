[project]
name = "strava-data-puller"
version = "0.1.0"
description = "Sync and pull Strava activities into DuckDB and Parquet"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31.0",
    "duckdb>=1.1.0",
]

[project.scripts]
strava-pull = "strava_pull:main"
